# Intake Edit Feature Wishlist

This file collects small feature and UX requests that should **not be implemented immediately**. Keep adding requests here until they are intentionally selected for a batch implementation pass.

## Pending

1. **Animate the Chapter List refresh icon while refreshing**
   - On the Chapter List screen, when chapter data is actively being fetched or refreshed, rotate the existing refresh icon continuously in place.
   - Keep the icon in the same button/location; only animate the graphic.
   - Stop the rotation and return the icon to its normal resting state when refreshing finishes.
   - Status: **Wishlist only — not implemented**
